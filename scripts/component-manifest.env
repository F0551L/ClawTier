# ClawTier component reference manifest.
# stable values should remain pinned and only change via intentional updates.

# OpenClaw Git refs
OPENCLAW_REF_STABLE=v2026.4.15
OPENCLAW_REF_EDGE=main

# Caddy container image refs
CADDY_IMAGE_STABLE=caddy:2.10.2-alpine
CADDY_IMAGE_EDGE=caddy:2-alpine
